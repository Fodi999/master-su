<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#4caf50">
  <title><%= title %></title>
  <style>
    /* Общие переменные */
    :root {
      --primary-color: #4caf50;
      --secondary-color: #282828;
      --light-bg: #f4f4f9;
      --font-family: 'Arial', sans-serif;
      --transition: all 0.3s ease-in-out;
    }

    /* Базовый стиль для всего документа */
    body {
      font-family: var(--font-family);
      background-color: var(--light-bg);
      color: var(--secondary-color);
      line-height: 1.6;
      margin: 0;
      transition: var(--transition);
    }

    /* Хедер */
    header {
      background: var(--secondary-color);
      color: white;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .header-container {
      display: flex;
      align-items: center;
      gap: 20px;
      max-width: 1200px;
      width: 100%;
      text-align: center;
    }

    .logo {
      height: 60px;
      width: auto;
    }

    header h1 {
      font-size: clamp(1.5rem, 4vw, 2.5rem);
      margin: 0;
    }

    /* Основной контент */
    main {
      padding: 20px;
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
    }

    main section {
      background: var(--secondary-color);
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    main section h2 {
      margin-bottom: 15px;
      font-size: 1.8rem;
      color: var(--primary-color);
    }

    main ul {
      list-style-type: disc;
      margin-left: 20px;
      padding: 0;
    }

    main ul li {
      margin: 8px 0;
      padding: 5px;
      border-radius: 5px;
      transition: var(--transition);
    }

    main ul li:hover {
      background: var(--primary-color);
      color: white;
      cursor: pointer;
    }

    /* Футер */
    footer {
      background: var(--secondary-color);
      color: white;
      text-align: center;
      padding: 10px;
      margin-top: 20px;
      font-size: 0.9rem;
    }

    footer p {
      margin: 0;
    }

    /* Карточки */
    .cards-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
      padding: 20px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .card {
      background: var(--secondary-color);
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    .card-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 10px 10px 0 0;
    }

    .card-content {
      padding: 20px;
    }

    .card-content h2 {
      margin-top: 0;
      font-size: 1.5rem;
      color: var(--primary-color);
    }

    .card-content p {
      margin: 10px 0;
      font-size: 1rem;
      color: #f4f4f9;
    }

    /* Адаптивность */
    @media (max-width: 600px) {
      .header-container {
        flex-direction: column;
      }

      .logo {
        height: 50px;
      }

      header h1 {
        font-size: 1.8rem;
      }

      main {
        padding: 15px;
      }

      main section {
        padding: 15px;
      }
    }

    /* Темная тема */
    @media (prefers-color-scheme: dark) {
      body {
        background-color: #121212;
        color: #e0e0e0;
      }

      header {
        background: var(--secondary-color);
        color: #f4f4f9;
      }

      main section {
        background: #1e1e1e;
        color: #f4f4f9;
      }

      footer {
        background: #1e1e1e;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="header-container">
      <img src="<%= logo %>" alt="Master Sushi Logo" class="logo">
      <h1><%= message %></h1>
    </div>
  </header>

  <main class="cards-container">
    <section class="card">
      <img src="/images/002.jpg" alt="Technologie" class="card-image">
      <div class="card-content">
        <h2>Najnowsze Technologie w Rozwoju Webowym</h2>
        <ul>
          <% technologies.forEach(function(tech) { %>
            <li><%= tech %></li>
          <% }); %>
        </ul>
      </div>
    </section>

    <section class="card">
      <img src="/images/003.jpg" alt="Sushi" class="card-image">
      <div class="card-content">
        <h2>Dlaczego My?</h2>
        <p>Oferujemy najlepsze sushi w mieście, przygotowane z miłością i najwyższej jakości składników.</p>
      </div>
    </section>

    <section class="card">
      <img src="/images/004.jpg" alt="Kontakt" class="card-image">
      <div class="card-content">
        <h2>Skontaktuj się z nami</h2>
        <p>Email: info@mastersushi.com</p>
        <p>Telefon: +1 234 567 890</p>
      </div>
    </section>
  </main>
  <%- include('partials/navigation') %>



  <script>
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.register('/sw.js').then(() => {
        console.log('Service Worker Registered');
      }).catch(err => {
        console.error('Service Worker Registration Failed:', err);
      });
    }
  </script>
</body>
</html>



